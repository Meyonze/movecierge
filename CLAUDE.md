# 転シェルジュ（movecierge） — Claude Code 引き継ぎ

Claude.ai（ブラウザ版）で設計・プロトタイピングしてきた個人開発プロジェクトです。
この文書はGitHubを拠点にした開発体制への移行にあわせた引き継ぎ用。
プロジェクトディレクトリ直下に `CLAUDE.md` として置くか、最初のメッセージにそのまま貼り付けてください。

## リポジトリとURL

- GitHub: `meyonze/movecierge`
- 公開URL: `https://meyonze.github.io/movecierge/`
- ホスティング: GitHub Pages

## 最初にやること（リポジトリのセットアップ）

1. `movecierge-prototype.html` を、リポジトリ直下に **`index.html`** として配置する
   （GitHub Pagesは `index.html` を自動でルートページとして扱うため、これでURLがそのまま
   `https://meyonze.github.io/movecierge/` になる）
2. `zenkoku_hikkoshi_research.py` も同リポジトリに入れる（`scripts/` などに配置推奨）
3. この `CLAUDE.md` もリポジトリ直下に置く
4. 簡単な `README.md` を用意する（プロジェクト概要、プロトタイプである旨、ローカルでの
   確認方法程度でよい）
5. git初期化 → 初回コミット → GitHubへpush
6. GitHub側の Settings → Pages で公開設定（ブランチ・フォルダを指定）
7. 数分待って `https://meyonze.github.io/movecierge/` で表示確認

## これは何か

旧住所・新住所の市区町村と、あてはまる条件（国保・介護・子ども・車・犬など）を入力すると、
必要な役所手続きだけに絞り込んだチェックリストを生成するツール。単一HTMLファイルで動作する。

- 転出届・転入届は全国共通ルール（転出はオンライン可、転入は窓口必須）で処理
- 市区町村ごとに違うのは「窓口の住所」「案内ページのURL」だけ、という3層構造で設計している
  1. 全国共通・非行政（銀行、保険、ライフラインなど） — 都市に依存しない
  2. 全国共通・行政ルール（転出届/転入届の期限・オンライン可否） — 都市に依存しない
  3. 都市固有（窓口情報・リンクURL） — ここだけ都市ごとのデータが要る
- 個人開発の収益化としては筋が悪いという結論は出ている（行政情報は誰でも見られる公開情報で
  独自の優位性がなく、引っ越しは低頻度でリピートしないため）。今は「型」を作る意味合いが強い。

## ブランディング

- 表示名: **転シェルジュ**（転出・転入の「転」＋コンシェルジュ）
- 英字コードネーム（URL・リポジトリ用）: **movecierge**（本体UIには英字を出さない方針）
- ロゴ: 藍色（`--accent:#22406B`）の四角に白文字で「転」。既存UIの「注」ラベルや必須項目の
  番号ボックスと同じ意匠を再利用したもの。faviconもインラインSVGで実装済み（外部ファイル不要）

## データの現状

- 候補は全国1,740自治体で、`data/cities.json` を唯一の自治体ID一覧としている。
- 公開用データは1,441件、レビュー用データは439件。画面には公開用データだけを使い、未収録の自治体は全国共通ルールと公式サイト検索への導線で案内する。
- 公開用データのうち、転出・転入のどちらかの公式URLが不足しているものは1件。URLや窓口情報を推測で埋めない。
- `zenkoku_hikkoshi_research.py` は `data/cities.json` を入力にし、画面と同じ「自治体IDをキーにしたJSON形式」で出力する。高信頼度かつ窓口・転出・転入の情報がそろったものだけを公開用へ入れ、その他はレビュー用へ分離する。

## 優先度順にやってほしいこと

### 1. 公式情報の品質改善

```bash
pip install anthropic --break-system-packages
export ANTHROPIC_API_KEY=sk-ant-...

# まず少数で試す
python zenkoku_hikkoshi_research.py --limit 20

# 問題なければ本番（中断しても再実行で続きから）
python zenkoku_hikkoshi_research.py --workers 4
```

- まず公開用データで転出・転入URLが不足している自治体を、公式ページの内容まで確認して補完する。
- レビュー用データは、`confidence: high` にできる根拠がそろったものだけ公開用へ反映する。再調査は `--retry-reviewed` を使う。
- スクリプトを走らせる前後に `node scripts/validate-data.mjs` を実行し、構造エラーがないことを確認する。

### 2. データ読み込み構成の維持

`index.html` は `data/cities.json` と `data/city_data.json` を実行時に読み込む。GitHub Pagesでそのまま配信でき、読み込みに失敗した場合は生成ボタンを無効化して案内を表示する。データの追加時は、この形式を崩さないこと。

### 3. 東京23区・政令指定都市以外の細かい抜け漏れチェック

94都市はすべて `hasData:true` になっているが、リンク切れ・住所変更などで情報が古くなる
可能性は常にある。定期的な再検証（3〜6ヶ月に一度など）も視野に入れておくとよい。

## 守ってほしい設計上の決めごと

- **単一ファイル・依存なし志向**（今後は index.html + city_data.json 程度までは許容）。
  フォントはWeb Fontで妥協しているが、JSライブラリは一切使っていない
- **ボタン起動の生成フロー**: 旧住所・新住所を変えたときは「チェックリストを作成する」
  ボタンを押すまで結果が更新されない（stale表示になる）。一方、属性チェック（国保・介護・
  子ども・車・犬）は生成済みの結果にも即座に反映される。この非対称は意図的な設計（住所変更は
  重い操作、属性は軽いフィルター、という区別）。安易に統一しないこと
- **「同じ窓口」ではなく「同じ施設」**と書くこと。建物内の窓口が別の課である可能性があるため、
  ユーザー指摘で修正済み。同じ表現の揺り戻しに注意
- **色は藍色（`--accent:#22406B`）＋薄グレー地**。テラコッタ＋クリーム色＋セリフ体という
  「いかにもAI生成」な配色パターンは一度指摘されて却下済みなので、絶対に戻さないこと
- **チェックボックス的な意匠を全要素で統一**（方向選択・属性フィルター・項目の完了マーク、
  ロゴマークまで含めて、すべて同じ「藍色の四角」という視覚言語で統一している）
- **属性フィルターは「常時表示3つ＋もっと見るで展開2つ」の構成**。今後さらに条件を追加する
  ときも、頻度が低いものは展開エリアに入れること（自己申告フィルター自体が負担にならないよう
  にする、という初期の設計判断に基づく）

## ハマった実際のバグ（教訓として残す）

`hidden` 属性を持つ要素が、同じ要素に付いた別のクラス（`display:flex` 等を指定するクラス）に
よって上書きされ、「非表示にならない」バグが発生した。CSSの詳細度としては同点でも、
author stylesheet が user-agent stylesheet に勝つため `hidden` 属性の効果が消えてしまう。
対策として

```css
[hidden]{display:none !important;}
```

をグローバルに入れてある。新しく `hidden` を使う要素を追加するときも、個別クラスで
`display` を上書きしていないか必ず確認すること。

## 参考にした市場リサーチ

- いえらぶGROUP（2024年3月）: 転出入手続きを「面倒」と回答した人 93.0%。マイナポータルでの
  「スマホで転出」の利用経験者は 17.2%（認知度48%の半分以下）
- 引越し侍（2015年、回答2,000件）: 面倒だった手続き1位はインターネット回線の切替
  （工事日程調整）。電気・水道では「お客様番号が分からず結局電話した」という声あり

これらを踏まえて、電気の項目に「お客様番号を用意しておく」tip、インターネット回線に
「要立会い」タグ、転出届の案内文に利用率データを使った行動喚起を入れてある。

## 収益化についての結論（繰り返し確認しておく）

単体でのマネタイズは筋が悪いという結論が出ている。行政情報は公開情報で独自優位性がなく、
引っ越しは低頻度でリピートしない。今後もし方向転換するなら、自前の集客より、既に引っ越し
関連の集客を持つ不動産会社・引越し業者へのチェックリスト機能の組み込みを検討する方が現実的、
という話が過去に出ている（営業活動が要るため、個人開発の時間対効果としては未着手）。
